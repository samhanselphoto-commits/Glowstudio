export default function HomePage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <div className="text-center">
        <p className="text-sm uppercase tracking-[0.3em] text-[color:var(--text-tertiary)]">
          Glowstudio
        </p>
        <h1 className="mt-3 text-5xl font-semibold tracking-tight text-[color:var(--text-primary)]">
          Skeleton ready.
        </h1>
        <p className="mt-3 text-base text-[color:var(--text-secondary)]">
          Waiting for hero image from Higgsfield + redesign plan.
        </p>
      </div>
    </main>
  );
}
